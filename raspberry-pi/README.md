# Raspberry Pi

该目录用于存放树莓派端的视觉识别、目标位置计算、任务逻辑、通信和运行脚本。

当前目录为协作占位目录，等待树莓派队友在独立功能分支中提交实现。

`f4-tube-controller/RPI/` 中现有的代码是与 F4 固件协议配套的解析和联调工具，
暂时随固件工程保存。后续若将协议模块迁移到本目录，应在同一个 Pull Request
中同步更新测试和文档。

建议后续补充：

- Python 版本与依赖文件
- 摄像头和视觉模型配置
- 启动命令与运行参数
- 与 F4、底板之间的通信协议
- 日志、测试和部署说明
